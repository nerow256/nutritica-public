# PostgreSQL database (use Neon for free hosting: https://neon.tech)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Optional: Ollama host for local AI (default: http://localhost:11434)
OLLAMA_HOST="http://localhost:11434"

# Optional: Gemini cloud AI (free fallback when Ollama is offline)
GEMINI_API_KEY=""
